<h1>Teams</h1>

<table class="table">
  <thead>
  <tr>
    <th>Name</th>
    <th>Slack channel</th>
    <th>Users</th>
    <th>Events</th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% @teams.each do |team| %>
    <tr>
      <td><%= link_to team.name, edit_team_path(team) %></td>
      <td><%= team.slack_channel %></td>
      <td><%= team.users.count %></td>
      <td><%= team.events.count %></td>
      <td class="text-right"><%= link_to 'X', team, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-light btn-sm' %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<p>
  <%= link_to 'New Team', new_team_path, class: 'btn btn-primary' %>
</p>